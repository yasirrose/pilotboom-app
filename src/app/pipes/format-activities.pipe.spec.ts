import { FormatActivitiesPipe } from './format-activities.pipe';

describe('FormatActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
